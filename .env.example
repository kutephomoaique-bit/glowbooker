# Database Configuration
DATABASE_URL=postgresql://username:password@hostname:port/database

# Session Configuration  
SESSION_SECRET=your-session-secret-key-here

# Object Storage Configuration
PUBLIC_OBJECT_SEARCH_PATHS=public
PRIVATE_OBJECT_DIR=private

# Environment
NODE_ENV=production

# Optional: Replit-specific variables (only needed if using Replit Auth)
# REPLIT_DOMAINS=
# ISSUER_URL=
# REPL_ID=
# REPLIT_DEPLOYMENT=

# Vercel Deployment Notes:
# 1. Set these environment variables in your Vercel project settings
# 2. Use Vercel environment variable references in vercel.json:
#    - DATABASE_URL: @database_url
#    - SESSION_SECRET: @session_secret
#    - REPLIT_DOMAINS: @replit_domains (if using Replit Auth)
#    - ISSUER_URL: @issuer_url (if using Replit Auth)
#    - PUBLIC_OBJECT_SEARCH_PATHS: @public_object_search_paths
#    - PRIVATE_OBJECT_DIR: @private_object_dir
# 3. The Express backend will run as serverless functions
# 4. Static frontend files are served directly by Vercel CDN